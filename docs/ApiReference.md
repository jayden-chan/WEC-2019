# Backend REST API reference

## Endpoints

### `/new` `POST`
Creates a new game
#### Params
`size`: The size of the board

#### Status
`400` if no `size` is provided

`200` otherwise





